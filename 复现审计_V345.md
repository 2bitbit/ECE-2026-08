# V3/V4/V5 复现审计报告（2026-08-18，本机 i5-11xxx 重跑）

> 代码来源：commit 9a5d925（CQL0329，2026-08-18 20:07「新增 V3/V4/V5 模型流水线、实测产物与跨版本对比」）
> 复现方式：本机完整重跑 `pipeline_v3/v4/v5.py`（默认参数：seed=0、batch=64、lr=1e-3，V3 20 轮 / V4 40 轮 / V5 40 轮）

## 一、三版本复现结果（本机实测）

| 版本 | 结构 | 参数量 | fp32 acc | int8 acc | p99 fp32 (ms) | p99 int8 (ms) | fp32/int8 体积 (kB) | 判定 |
|---|---|---|---|---|---|---|---|---|
| V3 | 128² + [32,64] + Flatten + 4×TTA | 412,038 | 0.8778 | 0.8806 | 8.23 | 6.73 | 1619.5 / 408.5 | FITS / FITS |
| V4 | 96² + [16,32,64,64] + GAP + Cutout | 60,614 | 0.8806 | 0.8750 | 0.78 | 1.36 | 254.9 / 70.2 | FITS / FITS |
| V5 | 128² + [32,64,128,128] + GAP + 4×TTA | 241,030 | 0.9306 | 0.9306 | 16.83 | 10.33 | 959.7 / 246.9 | **TOO SLOW / FITS** |

## 二、三方数字对比（docx vs 仓库提交 vs 本机复现）

| 指标 | docx 宣称 | 仓库提交 (9a5d925) | 本机复现 |
|---|---|---|---|
| V3 fp32 / int8 acc | 86.39 / 86.11 | 0.8639 / 0.8472 | **0.8778 / 0.8806** |
| V3 p99 (fp32/int8) | 11.95 / 13.54 ms | 7.55 / 8.26 ms | 8.23 / 6.73 ms |
| V4 fp32 / int8 acc | 88.61 / 88.06 | 0.8861 / 0.8306 | **0.8806 / 0.8750** |
| V4 p99 (fp32/int8) | 0.63 / 1.08 ms | 0.60 / 0.89 ms | 0.78 / 1.36 ms |
| V5 fp32 / int8 acc | 92.78 / 92.50 | 0.9278 / 0.9194 | **0.9306 / 0.9306** |
| V5 p99 (fp32/int8) | 16.30 / 10.95 ms | 14.40 / 12.40 ms | 16.83 / 10.33 ms |

## 三、关键结论

1. **V5 fp32 超过 15ms 预算**：本机复现 16.83ms（TOO SLOW）、docx 16.30ms（TOO SLOW）一致；
   仓库提交的 14.40ms（FITS）是**该机器单次运行值**，fp32 形态在 15ms 边界上不稳定。
   **唯一可靠部署形态是 int8（本机 10.33ms，留 31% 余量）。** docx 的判断是对的。
2. **V5 是本机唯一 fp32/int8 双形态均超 baseline 精度的版本**：int8 0.9306 vs baseline 0.8583（+7.2pt），
   且 int8 相对 fp32 零掉点（0.9306 = 0.9306）。V5 int8 是整体最佳交付候选。
3. **跨机器不可复现**：三版本 fp32 精确度在不同机器上相差 0.3~1.4pt（如 V3 仓库 0.8639 vs 本机 0.8778），
   int8 更敏感（V4 int8 仓库 0.8306 vs 本机 0.8750，差 4.4pt）。训练轨迹随 torch 版本/机器漂移，属已知现象
   （v2 复现审计已记录同因），答辩需标注"单机实测值"。
4. **V4 的"学术优雅"在本机未能复现 docx 的 int8 88.06%**：本机 int8 0.8750，掉点 0.6pt（远好于仓库 5.5pt 掉点）。
   V4 量化鲁棒性跨机器极不稳定——答辩时应避免用 V4 的量化数字作卖点。

## 四、修复的 bug（本次提交）

1. **pipeline_v4.py / pipeline_v5.py 产物不完整**：`write_results` 不写 `results_table.md`（仅有 csv），
   且无 `latency.png` 图（V3 有）。已补：md 输出块 + `fig_latency()`（boxplot，含 15ms 红线）+ main 调用。
   - 修复前 V4/V5 产物：csv + per_class + 2 图（缺 md、缺 latency.png）
   - 修复后：8 个文件齐全（csv/md/per_class/2 模型/3 图）
2. **comparison_v345.md 的 V4/V5 int8 数字与产物不一致**：当时标注"V4/V5 未写 results_table.md"，
   现在产物已补全，数字以重跑后为准。

## 五、图表有效性

- `v3/v4/v5_output/figures/*.png` 共 8 张全部 PIL verify 通过（含新增 latency.png）
- 新增 `figures/cross_version_comparison.png`：baseline vs V3/V4/V5 三面板（准确率/延迟 log/体积），
  红色虚线标 15ms 预算，可作答辩总览图

## 六、遗留建议（需成员确认）

1. **V5 部署形态**：明确答辩口径为「fp32 超预算（16.8ms），int8 10.3ms 是部署形态」——docx 已这么写，与实测一致 ✓
2. **数字口径**：docx 中的 V3/V4 int8 数字（86.11 / 88.06）与任何机器实测都不符，建议修正为
   「单机实测值」并标注机器，防止答辩被追问
3. **真机验证**：V5 在树莓派 5（ARM）上的延迟未验证，int8 在 ARM 上通常更快，建议安排丙同学真机测试
4. 若采用 V5 作为最终交付，需同步更新 README / final_results_table / PPT 大纲（当前 README 仍是 baseline 数字）